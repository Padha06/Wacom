{
  "name": "signing-server",
  "version": "1.0.0",
  "private": true,
  "description": "Centralized treasury signature capture server (Railway/Vercel)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5"
  },
  "engines": {
    "node": ">=18"
  }
}
